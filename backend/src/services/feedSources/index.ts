/**
 * Feed Sources Index — import all sources to register them.
 * Add new sources here when they're ready.
 */

// Phase-1 Network sources (people)
import './friendCheckins';
import './friendPhotos';
import './friendWishlists';
import './profileInteractions';

// Phase-1 Community sources (things)
import './rigUpdates';
import './campgroundUpdates';
import './threadActivity';
import './communityTripKits';

// Existing content (routed by actor)
import './boardPosts';
